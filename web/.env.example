# Baked in at build time (NEXT_PUBLIC_* only). Also reads the repo-root .env (see next.config.ts).
NEXT_PUBLIC_REGISTRY_URL=http://localhost:4000
# NEXT_PUBLIC_CONTRIBUTOR_IMAGE=ghcr.io/your-org/raven-contributor   # shown in the copy-paste run command
# NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com
# NEXT_PUBLIC_SOLANA_CLUSTER=devnet
